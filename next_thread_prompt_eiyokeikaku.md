## 設計判断メモ追記（2026-08-07）専門家フィードバック対応

### 対応方針一覧

| # | 指摘内容 | 対応方針 | 優先度 |
|---|---|---|---|
| 1 | 海藻類（ひじき・わかめ）が赤群→緑群の誤り | 食品マスタの三色食品群属性を修正。他の海藻類（こんぶ・のり等）も併せて確認 | 中（独立修正） |
| 2 | 栄養素表示順（成分表準拠） | 対応不要（現状維持） |
| 3 | 帳票の列位置揃え | CSS対応（table-layout: fixed等） | 低 |
| 4 | 商品名表示 | 対応不要（現状維持） |
| 5-a | 調味料0.1g単位で計算したい | 分量カラムをdecimal（小数第1位）に変更 | 高（第一段階） |
| 5-b | 分量だけ後から編集したい | 一覧からのインライン編集または簡易編集UI | 高（第一段階） |
| 5-c | 料理ごとにグルーピング表示したい | Dish独立管理（下記） | 高（第一段階） |

### Dish（料理）独立管理の設計方針

**背景**: 献立の管理単位は「栄養素」でも「食品材料」でもなく「料理」である、という原則に基づく。

**将来形（4階層）**:

Dish（料理マスタ）
└ has_many :dish_ingredients
DishIngredient（標準レシピ＝料理マスタの標準分量）
└ dish_id, food_id, standard_amount（decimal, 0.1g単位）
DailyMenu（1日の献立）
└ has_many :daily_menu_dishes
DailyMenuDish（献立内の料理＝Dishマスタへの参照＋表示順）
└ daily_menu_id, dish_id, position
MenuItem（実際に使った食品明細）
└ daily_menu_dish_id, food_id, amount（decimal, 0.1g単位）

**第一段階（今回実装スコープ）**:
- `Dish` + `DailyMenuDish` + `MenuItem`（dish_id経由）のみ実装
- 料理名の登録・献立への組み込み・料理ごとのグルーピング表示まで
- `DishIngredient`（標準レシピのコピー機能）は含めない
- 分量カラムのdecimal化（0.1g単位）も第一段階に含める

**第二段階（後日・スコープ外）**:
- 頻出する料理を「料理マスタとして保存」する逆生成機能（既存のDailyMenuDishから標準レシピを作る）
- 実装時の最重要検証項目：**コピーであって参照ではないこと**。DailyMenuDishがDishIngredientを直接参照する実装になると、標準レシピ側の修正が過去の献立の栄養価まで変えてしまう。栄養計算の正確性に直結するため重点的にテストする。

### 保留事項

- 画面遷移図：週単位表示の要否、保存後の遷移先（show画面へ自動遷移 or 別ボタン）の2点は、専門家から明確な要望が出ていないため保留。次回の専門家ヒアリング時に確認する。

### 次のアクション（第一段階完了後）

- 第一段階（Dish + DailyMenuDish + MenuItem実装、0.1g単位decimal化）完了後、ConoHa VPSへデプロイして専門家（管理栄養士）に評価を依頼する
- デプロイ前チェック：
  - マイグレーション（Dish関連・decimal化）をMac mini実行後、ConoHa側にも適用
  - 既存の献立データがDish未設定のまま表示崩れしないか確認
  - Rails CVE（Active Storage, 8.1系）対応の要否をこのタイミングで判断
  - Basic認証（eiyo_user）は継続、Devise化は次フェーズのまま
- 専門家評価の際は、今回のフィードバック5項目（特に5-a〜c）がどう改善されたかの変更点サマリを用意すると評価がスムーズ

## デプロイ記録（2026-08-08）第一段階 ConoHa本番反映・専門家評価準備完了

### 実施内容
- 第一段階実装（Dish独立管理・0.1g decimal化・分量インライン編集・帳票列揃え・海藻類食品群修正）をConoHa VPSにデプロイ
- マイグレーション適用、既存データ（MealPlanDish 10件）は無事故で移行

### デプロイ中に発見・修正した不具合（3件）

1. **「参照でなくコピー」問題（設計メモが警告していたリスクが実際に存在）**
   - 既存のNutritionCalculatorがMealPlanDish→Dish→DishIngredientを直接参照する実装になっており、標準レシピ編集で過去の全献立の栄養価が変わってしまう状態だった
   - MenuItemモデルを新設し、Dish追加時にDishIngredientからMenuItemへコピー生成する方式に修正して解消

2. **0g分量でのDish追加時に自動コピー処理が失敗する不具合**
   - MenuItemモデルに`greater_than: 0`のバリデーションがあり、専門家評価用サンプルデータ（分量仮値0.0g）投入時、Dish追加の自動コピー処理が例外で中断し、MealPlanDishだけが作成されMenuItemが空の中途半端な状態になった
   - バリデーションを`greater_than_or_equal_to: 0`に緩和
   - Dish追加処理全体をトランザクションで囲み、失敗時は全体ロールバックするよう修正（再発防止）
   - 本番の壊れたデータ復旧用に`maintenance:backfill_missing_menu_items`rakeタスクを作成・実行し、影響を受けた2件（MealPlanDish#1, #3）を復旧

3. **帳票印刷（print_hancho）・Download MDが栄養価0のまま**
   - 詳細画面はMenuItem経由で正しく計算されていたが、印刷帳票とMDダウンロードが旧ロジック（meal_plan_items）を参照したままだった
   - 両方をMenuItem/NutritionCalculator経由に修正し、詳細画面と一致する計算結果になることを確認

### UI改善
- 「編集」リンクのラベルを「料理を変更」に変更（分量編集画面と紛らわしかったため）

### 専門家評価用サンプルデータ
- 和知学校給食センター発行の2026年6月献立表（PDF）より、料理名・食材構成のみを抽出して6料理を投入（分量は仮値0.0g、専門家評価時に実測値へ差し替え想定）
  - 主菜：とりにくのマスタードやき／にくどうふ／チャプチェ／さばのみそに
  - 副菜：かいそうサラダ／ちくわのいそべあげ
- 食材マッチングで代替判断が必要だった項目：米油→調合油(id 4505)、パプリカ(生野菜)→赤ピーマン(id 3250)、かいそう→わかめ

### 現在の状態
- ConoHa本番（163.44.114.31:8080）で全機能（5-a〜5-c、印刷、MDダウンロード）動作確認済み
- 専門家への評価依頼文を作成済み、送付待ち

### 次のアクション
- 専門家からの評価フィードバック待ち
- フィードバックを受けて、実際の分量データ入力（サンプルは仮値0.0gのまま）
- 第二段階（DishIngredientの逆生成機能）は専門家評価後に着手判断
